<h1>File Upload</h1>
<%= form_for(:upload, :url => { :action => 'uploadFile' }, 
                        :multipart => true) do |form| %>
<p>
	<!-- <%= form.label :datafile, "Select File" %> :  -->
	<!-- <%= form.file_field 'datafile' %> -->
	<%= form.text_area :log, placeholder: "Compose new log..." %>
</p>
<%= form.submit "Upload" %>
<% end %>